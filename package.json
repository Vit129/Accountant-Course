{
  "name": "accountant-learning",
  "private": true,
  "description": "Dev-only tooling for maintaining this CPA course (self-test harness). End users never need this - open index.html directly, no install required.",
  "scripts": {
    "test": "node shared/selftest.mjs",
    "start": "bash scripts/release.sh"
  },
  "version": "0.5.0"
}
